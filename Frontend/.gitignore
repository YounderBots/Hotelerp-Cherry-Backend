# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static
# package-lock.json is deliberately NOT ignored. CI runs `npm ci`, which
# requires the lockfile to be committed, and without it every install
# resolves dependencies afresh -- so the versions CI tests are not the
# versions anyone else gets.
